<tenantRegListenerServers>
    <server>
        <!-- governance -->
        <serverUrl>https://<%= governance_subdomain %>.<%= stratos_domain %>/services/</serverUrl>
		<userName>admin</userName>
		<password>admin</password>        
    </server>
    <server>
        <!-- identity -->
        <serverUrl>https://<%= is_subdomain %>.<%= stratos_domain %>/services/</serverUrl>
		<userName>admin</userName>
		<password>admin</password>        
    </server>
    <server>
        <!-- gadget -->
        <serverUrl>https://<%= gs_subdomain %>.<%= stratos_domain %>/services/</serverUrl>
		<userName>admin</userName>
		<password>admin</password>        
    </server>
    <server>
        <!-- app server -->
        <serverUrl>https://<%= as_subdomain %>.<%= stratos_domain %>/services/</serverUrl>
		<userName>admin</userName>
		<password>admin</password>        
    </server>
	<server>
        <!-- mashup server -->
        <serverUrl>https://<%= ms_subdomain %>.<%= stratos_domain %>/services/</serverUrl>
		<userName>admin</userName>
		<password>admin</password>        
    </server>
	<!-- bam -->
	<server>
        <serverUrl>https://<%= bam_subdomain %>.<%= stratos_domain %>/services/</serverUrl>
		<userName>admin</userName>
		<password>admin</password>        
    </server>
	<!-- brs -->
	<server>
        <serverUrl>https://<%= brs_subdomain %>.<%= stratos_domain %>/services/</serverUrl>
		<userName>admin</userName>
		<password>admin</password>        
	</server>
	<!-- esb -->
	<server>
        <serverUrl>https://<%= esb_subdomain %>.<%= stratos_domain %>/services/</serverUrl>
		<userName>admin</userName>
		<password>admin</password>        
    </server>
	<!-- dss -->
	<server>
        <serverUrl>https://<%= dss_subdomain %>.<%= stratos_domain %>/services/</serverUrl>
		<userName>admin</userName>
		<password>admin</password>        
    </server>
	<!-- bps -->
	<server>
        <serverUrl>https://<%= bps_subdomain %>.<%= stratos_domain %>/services/</serverUrl>
		<userName>admin</userName>
		<password>admin</password>        
    </server>
	<server>
        <serverUrl>https://<%= cep_subdomain %>.<%= stratos_domain %>/services/</serverUrl>
		<userName>admin</userName>
		<password>admin</password>        
    </server>
	<server>
        <serverUrl>https://<%= mb_subdomain %>.<%= stratos_domain %>/services/</serverUrl>
		<userName>admin</userName>
		<password>admin</password>        
    </server>
</tenantRegListenerServers>
